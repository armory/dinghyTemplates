{
  "name": "{{ var "name" ?: "Deploy" }}",
  "type": "deploy",
  "refId": "{{ var "refId" }}",
  "requisiteStageRefIds": {{ var "requisiteStageRefIds" ?: [] }},

  "clusters": {{ var "clusters" ?: [] }}
}