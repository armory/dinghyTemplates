{
  "name": "{{ var "name" ?: "wait" }}",
  "type": "wait",
  "refId": "{{ var "refId" }}",
  "requisiteStageRefIds": {{ var "requisiteStageRefIds" ?: [] }},

  "waitTime": {{ var "waitTime" ?: 30 }}
  "notifications": {{ var "notifications" ?: [] }}
  "skipWaitText": "{{ var "skipWaitText" ?: "Skipped" }}"
}